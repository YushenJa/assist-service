/* Здесь удобно держать помощников:

создаем NAVIGATE-действия (deep-links) по шаблону:

    например, „открыть список файлов со статусом ocr_failed за октябрь“.

создаем MUTATION-действия:

    строго из белого списка (например, retry-ocr, create-export),

    заполняют label, ссылку на бизнес-эндпойнт, метод, флаг requiresConfirm.

Зачем выносить отдельно: чтобы deep-links и quick-actions формировались одинаково по всей системе и их было легко поддерживать/менять. */