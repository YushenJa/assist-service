/* Что тут:
Описываешь типы/структуры данных для ассистента:

* как выглядит вход для логики (message, mode, context, locale, userInfo);
* как выглядит выход (answer, citations, actions, meta).

Зачем: чтобы всё ядро работало с одним формальным видом данных. */