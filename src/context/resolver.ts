/* Смысл:

1. Принимает:

    * данные о пользователе/аккаунте (из JWT/заголовков),
    * context из запроса (fileIds, txIds, dateRange).

2. Проверяет:

    * имеет ли пользователь доступ к этому аккаунту;
    * не превышены ли лимиты (количество id, ширина диапазона дат).

3. Загружает данные:

    * сейчас — может возвращать пустой список или крошечную заглушку (так как SQL ещё нет);
    * позже — реальные данные из SQL/Firestore.

4. Маскирует персональные данные:

    * e-Mail, IBAN, телефоны — частично;
    * координаты — округляет;
    * заметки/описания — режет по длине.

5. Возвращает аккуратный объект, который можно без страха отдавать в AI или использовать в FAQ.*/