/* Чтобы у тебя был один файл, где видно все пути, которые понимает сервис.

В нашем случае главный маршрут — это: POST /support/assist

Что там по смыслу:

ты «регистрируешь» маршрут POST /support/assist;
для него задаёшь цепочку:
проверки (auth, rate-limit — если хочется),
валидация тела запроса,
вызов «мозга» ассистента из src/core,
возврат ответа в виде JSON.

Важно: routes.ts не должен сам думать, как отвечать. Он только связывает HTTP-запрос с правильной функцией из логики.*/