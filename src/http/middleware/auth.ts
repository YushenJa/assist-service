/*Что делает:

* читает JWT из заголовка (например, Authorization), если токен уже проверен на Gateway, можно использовать проброшенную информацию (userId, accountId, роли);
* либо проверяет ещё раз подпись/claims (issuer, audience, exp), если вы так решили;
* проверяет, что у пользователя есть право пользоваться ассистентом (минимальный scope – только чтение);
* если что-то не так — возвращает ошибку и не пропускает дальше.

Зачем: никакая логика ассистента не должна работать без корректной аутентификации. */