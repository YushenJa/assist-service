/*Что делает по смыслу:

при каждом запросе:
    создаёт или читает requestId
    пишет в лог: метод, путь, requestId, время старта;
после обработки — время выполнения и статус-код.

Зачем: легче искать проблемы и строить метрики.*/

import { Request, Response, NextFunction } from 'express'
