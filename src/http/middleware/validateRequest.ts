/*Что делает:

проверяет, что:

* message — строка и не слишком длинная (например, до 2000 символов);

* mode — либо "FAQ", либо "AI";

* context — объект, внутри:

    * fileIds и txIds — массивы строк и не больше N штук;

    * dateRange — есть from и to в правильном формате, и from <= to.

* если не проходит — возвращает ошибку 400 INVALID_CONTEXT или 400 INVALID_MODE.

Зачем: чтобы дальше, внутри логики ассистента, работал уже с корректными данными и не думал о мусоре. */